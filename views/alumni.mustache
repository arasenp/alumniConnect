<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alumni Connect</title>
    <script src="https://code.jquery.com/jquery-3.7.1.slim.js" integrity="sha256-UgvvN8vBkgO0luPSUl2s8TIlOSYRoGFAX4jlCIm9Adc=" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <style>
        body,
        html {
            margin: 0;
            padding: 0;
            height: 100%;
            font-family: Arial, sans-serif;
            background-color: #DDF2FD;
        }

        nav {
            background-color: #427D9D;
            padding: 10px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            width: 50%;
            border-radius: 15px;
            margin-top: 20px;
        }

        .navbar-brand {
            font-size: 24px;
            color: #DDF2FD;
            text-decoration: none;
        }

        .navbar-nav {
            list-style: none;
            margin: 0;
            padding: 0;
            display: flex;
        }

        .nav-item {
            margin-right: 20px;
        }

        .nav-link {
            text-decoration: none;
            color: #DDF2FD;
            font-size: 18px;
        }

        .nav-link:hover {
            border-bottom: 2px solid #DDF2FD;
            padding-bottom: 3px;
        }

        .wrapper {
            min-height: 100%;
        }

        .content {
            padding-bottom: 200px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .footer {
            background-color: #DDF2FD;
            color: #427D9D;
            border: 10px solid #427D9D;
            position: fixed;
            bottom: 0;
            width: 100%;
            box-sizing: border-box;
        }

        .footer-container {
            display: flex;
            justify-content: space-around;
            align-items: center;
        }

        .footer-column {
            flex: 1;
            text-align: center;
        }

        .form-control {
            padding: 10px;
            margin-right: 10px;
            border: 1px solid #9BBEC8;
        }

        .subscribe-btn {
            background-color: #9BBEC8;
            color: #427D9D;
            padding: 10px;
            border: none;
            cursor: pointer;
        }

        .subscribe-btn:hover {
            background-color: #427D9D;
            color: #DDF2FD;
        }

        .social-link {
            margin-right: 10px;
        }

        .modal {
            display: none;
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background-color: #DDF2FD;
            padding: 20px;
            border-radius: 15px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }

        .modal-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
        }

        .modal-header,
        .modal-body,
        .modal-footer {
            margin-bottom: 15px;
        }

        .modal-header h5 {
            margin: 0;
            font-size: 24px;
            color: #427D9D;
        }

        .modal-body p {
            margin: 0;
            color: #427D9D;
        }

        .modal-footer button {
            background-color: #9BBEC8;
            color: #427D9D;
            padding: 10px;
            border: none;
            cursor: pointer;
        }

        .modal-footer button:hover {
            background-color: #427D9D;
            color: #DDF2FD;
        }

        .events-container {
            background-color: #DDF2FD;
            padding: 20px;
            margin-top: 20px;
            border-radius: 15px;
            width: 50%;
        }

        .events-row {
            display: flex;
            justify-content: space-around;
            margin-top: 20px;
        }

        .events-section {
            flex: 1;
            margin-right: 20px;
            padding: 15px;
            border-radius: 15px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            background-color: #427D9D;
            color: #DDF2FD;
        }

        .events-section h3 {
            text-align: center;
        }

        .event-category {
            margin-top: 20px;
        }

        .event-category h5,
        .event-category p {
            margin: 0;
        }

        .event-category {
            margin-top: 20px;
        }

        .event-category h5,
        .event-category p {
            margin: 0;
        }

        .action-container {
            display: flex;
            justify-content: space-around;
            margin-top: 20px;
        }

        .action-container .btn {
            font-size: 18px;
            padding: 10px 20px;
            border: none;
            cursor: pointer;
            border-radius: 5px;
            color: #DDF2FD;
        }

        .btn-primary {
            background-color: #17594A;
        }

        .btn-warning {
            background-color: #8EAC50;
        }

        .btn-danger {
            background-color: #D3D04F;
        }

        .welcome{
            color: #427D9D;
            font-size: 25px;
            text-align: center;
        }

        
    </style>
</head>

<body>

    <div class="content">
        <nav>
            <a class="navbar-brand" href="/">Alumni Connect</a>
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="#">About Us</a>
                </li>
            </ul>
        </nav>

        <div class="welcome">
            <h1>Welcome to Alumni Connect Dashboard, {{ username }}.</h1>
        </div>

        <div class="events-container">

            <div class="action-container">
                <div class="container mt-5">
                    <h2>Add an Event</h2>
                    <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#addEventModal">Add Event</button>
                </div>

                <div class="container mt-5">
                    <h2>Modify an Event</h2>
                    <button type="button" class="btn btn-warning" data-toggle="modal" data-target="#modifyEventModal"
                        onclick="prepareModifyEvent('{{ eventName }}')">Modify Event</button>
                </div>

                <div class="container mt-5">
                    <h2>Remove an Event</h2>
                    <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#removeEventModal">Remove
                        Event</button>
                </div>
            </div>
        </div>

        <div class="events-container">
            <h2>Your Events</h2>
            <div class="events-row">
                <!-- Event Category: Professional Development -->
                <div class="events-section">
                    <h3>Professional Development</h3>
                    <div class="event-category">
                        <!-- Replace with your actual content from the backend -->
                        <h5>Event Name</h5>
                        <p>Date: Event Date</p>
                        <p>Created by: Event Creator</p>
                        <p>Description: Brief event description.</p>
                    </div>
                </div>

                <!-- Event Category: Networking -->
                <div class="events-section">
                    <h3>Networking</h3>
                    <div class="event-category">
                        <!-- Replace with your actual content from the backend -->
                        <h5>Event Name</h5>
                        <p>Date: Event Date</p>
                        <p>Created by: Event Creator</p>
                        <p>Description: Brief event description.</p>
                    </div>
                </div>

                <!-- Event Category: Campus Events -->
                <div class="events-section">
                    <h3>Campus Events</h3>
                    <div class="event-category">
                        <!-- Replace with your actual content from the backend -->
                        <h5>Event Name</h5>
                        <p>Date: Event Date</p>
                        <p>Created by: Event Creator</p>
                        <p>Description: Brief event description.</p>
                    </div>
                </div>
            </div>
        </div>

    </div>

    <!-- Add Event Modal -->
    <div class="modal" id="addEventModal">
        <div class="modal-overlay"></div>
        <div class="modal-content">
            <div class="modal-header">
                <h5>Add Event</h5>
            </div>
            <div class="modal-body">
                <form>
                    <p>Event Name</p>
                    <input type="text" class="form-control" name="eventName">
                    <p>Date</p>
                    <input type="date" class="form-control" name="eventDate">
                    <p>Event Category</p>
                    <select class="form-control" name="eventCategory">
                        <option value="professional">Professional Development</option>
                        <option value="networking">Networking</option>
                        <option value="campus">Campus Event</option>
                    </select>
                    <p>Event Description</p>
                    <textarea class="form-control" name="eventDescription" rows="3" placeholder="Enter event description"></textarea>
                </form>
            </div>
            <div class="modal-footer">
                <button type="submit">Add Event</button>
            </div>
        </div>
    </div>

    <!-- Modify Event Modal -->
    <div class="modal" id="modifyEventModal">
        <div class="modal-overlay"></div>
        <div class="modal-content">
            <div class="modal-header">
                <h5>Modify Event</h5>
            </div>
            <div class="modal-body">
                <form>
                    <p>Event Name</p>
                    <input type="text" class="form-control" name="eventName">
                    <p>Date</p>
                    <input type="date" class="form-control" name="eventDate">
                    <p>Event Category</p>
                    <select class="form-control" name="eventCategory">
                        <option value="professional">Professional Development</option>
                        <option value="networking">Networking</option>
                        <option value="campus">Campus Event</option>
                    </select>
                    <p>Event Description</p>
                    <textarea class="form-control" name="eventDescription" rows="3" placeholder="Enter event description"></textarea>
                </form>
            </div>
            <div class="modal-footer">
                <button type="submit">Save Changes</button>
            </div>
        </div>
    </div>

    <!-- Remove Event Confirmation Modal -->
    <div class="modal" id="removeEventModal">
        <div class="modal-overlay"></div>
        <div class="modal-content">
            <div class="modal-header">
                <h5>Remove Event</h5>
            </div>
            <div class="modal-body">
                <p>Are you sure you want to remove this event?</p>
            </div>
            <div class="modal-footer">
                <button type="button" id="confirmRemoveEventButton">Remove</button>
            </div>
        </div>
    </div>

    <footer class="footer">
        <div class="footer-container">
            <div class="footer-column">
                <h4>Contact</h4>
                <p>Email: alumniConnect@alueducation.com</p>
                <p>Phone: +230 5987 0546</p>
            </div>
            <div class="footer-column">
                <h4>Stay Tuned</h4>
                <a href="#" class="social-link" target="_blank"><img src="images/facebook.png" alt="Facebook"></a>
                <a href="#" class="social-link" target="_blank"><img src="images/twitter.png" alt="Twitter"></a>
                <a href="#" class="social-link" target="_blank"><img src="images/instagram.png" alt="Instagram"></a>
            </div>
            <div class="footer-column">
                <h4>Talk to us!</h4>
                <form>
                    <div class="input-group">
                        <input type="text" class="form-control" placeholder="Your email">
                        <button class="subscribe-btn" type="button">Subscribe</button>
                    </div>
                </form>
            </div>
        </div>
    </footer>

    <script>
        
        const addEventButton = document.querySelector('.btn-primary');
        const modifyEventButton = document.querySelector('.btn-warning');
        const confirmRemoveEventButton = document.querySelector('.btn-danger');

        addEventButton.addEventListener('click', () => {
            document.getElementById('addEventModal').style.display = 'block';
        });

        modifyEventButton.addEventListener('click', () => {
            document.getElementById('modifyEventModal').style.display = 'block';
        });

        confirmRemoveEventButton.addEventListener('click', () => {
            document.getElementById('removeEventModal').style.display = 'block';
        });

        function prepareModifyEvent(eventName) {
            const modifyEventForm = document.getElementById('modifyEventForm');
            const actionUrl = `/alumni/modifyEvent/${eventName}`;
            modifyEventForm.setAttribute('action', actionUrl);
        }
    </script>
</body>

</html>

